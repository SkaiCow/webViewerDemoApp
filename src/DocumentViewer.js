import React, {useEffect, useRef, useState} from 'react';

import SidePanelControls from './sidePanelControls.js';

const DocumentViewer = (props)=>
{
  return(
    <div>
      <SidePanelControls/>
    </div>
  );
}
export default DocumentViewer;
