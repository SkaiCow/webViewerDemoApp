import React, {useEffect, useRef, useState} from 'react';
import WebViewer from '@pdftron/webviewer';

const PDFContainer = (props) =>
{
  
  //styles for WebViewer

  return(

  );
}
export default PDFContainer;
